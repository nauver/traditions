const LANGS=['en','fr','nl'];let lang=localStorage.getItem('tradLang')||'en';let quiz=[],current=0,score=0,answered=false,timer=null,timeLeft=20,mode='classic',teamA=0,teamB=0;const TOTAL=30,app=document.getElementById('app');const t=k=>(TRANSLATIONS[lang]&&TRANSLATIONS[lang][k])||TRANSLATIONS.en[k]||k;function shuffle(a){const arr=[...a];for(let i=arr.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[arr[i],arr[j]]=[arr[j],arr[i]]}return arr}function pick(){const out=[],cc={};for(const q of shuffle(QUESTIONS)){if((cc[q.country]||0)>=2)continue;out.push(q);cc[q.country]=(cc[q.country]||0)+1;if(out.length===TOTAL)break}return shuffle(out)}function langs(){return `<div class="langs">${LANGS.map(l=>`<button class="${l===lang?'active':''}" data-lang="${l}">${l.toUpperCase()}</button>`).join('')}</div>`}function top(){return `<div class="top"><div class="logoBox"><img class="corLogo" src="assets/CoR_logo.png" alt="CoR logo"></div><h1 class="mainTitle">${t('title')}</h1>${langs()}</div>`}function bindLang(){document.querySelectorAll('[data-lang]').forEach(b=>b.onclick=()=>{lang=b.dataset.lang;localStorage.setItem('tradLang',lang);quiz.length?render():home()})}function home(){clearInterval(timer);quiz=[];app.innerHTML=`<div class="shell"><div class="stage">${top()}<div class="content homeContent"><div><h2 class="headline">${t('title')}</h2><div class="modeRow"><button class="smallBtn active" data-mode="classic">Classic</button><button class="smallBtn" data-mode="teams">${t('teams')}</button></div></div><button id="start" class="primary blueBtn">${t('start')}</button></div><div class="footer"><span>CoR gamification prototype V1.2</span><span>120 questions / 27 EU Member States</span></div></div></div>`;bindLang();document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-mode]').forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.mode});document.getElementById('start').onclick=start}function start(){quiz=pick();current=0;score=0;teamA=0;teamB=0;render()}function render(){clearInterval(timer);answered=false;const q=quiz[current];app.innerHTML=`<div class="shell"><div class="stage">${top()}<div class="content"><div class="status"><div class="pill">${t('question')} ${current+1} ${t('of')} ${quiz.length}</div><div class="pill">${t('score')}: <strong>${score}</strong></div><div class="pill timer" id="timerBox">20</div></div><div class="progress"><div style="width:${(current/quiz.length)*100}%"></div></div>${mode==='teams'?`<div class="teamScores"><button id="teamA">${t('teamA')}: ${teamA}</button><button id="teamB">${t('teamB')}: ${teamB}</button></div>`:''}<div class="meta"><span>${q.country}</span><span>${q.theme}</span><span>${q.difficulty}</span></div><h2 class="questionText">${q.question}</h2><div class="answers" id="answers"></div><div id="feedback" class="feedback hidden"></div><div class="nextArea"><button id="nextButton" class="primary hidden">${current+1===quiz.length?t('finish'):t('next')}</button></div></div><div class="footer"><span>Europe’s Traditions Challenge</span><span>CoR visual style</span></div></div></div>`;bindLang();if(mode==='teams'){document.getElementById('teamA').onclick=()=>{teamA++;document.getElementById('teamA').textContent=`${t('teamA')}: ${teamA}`};document.getElementById('teamB').onclick=()=>{teamB++;document.getElementById('teamB').textContent=`${t('teamB')}: ${teamB}`}}const ans=document.getElementById('answers');shuffle(q.answers).forEach(a=>{const b=document.createElement('button');b.className='answer';b.textContent=a;b.onclick=()=>reveal(a);ans.appendChild(b)});document.getElementById('nextButton').onclick=next;startTimer()}function startTimer(){timeLeft=20;timer=setInterval(()=>{timeLeft--;const el=document.getElementById('timerBox');if(el)el.textContent=timeLeft;if(timeLeft<=0){clearInterval(timer);reveal(null)}},1000)}function reveal(sel){if(answered)return;answered=true;clearInterval(timer);const q=quiz[current],ok=sel===q.correctAnswer;if(ok)score++;document.querySelectorAll('.answer').forEach(b=>{b.classList.add('disabled');if(b.textContent===q.correctAnswer)b.classList.add('correct');if(sel&&b.textContent===sel&&!ok)b.classList.add('wrong')});const fb=document.getElementById('feedback');fb.innerHTML=`<strong>${sel===null?t('timeout'):ok?t('correct'):t('wrong')}.</strong><br>${q.explanation}`;fb.classList.remove('hidden');document.getElementById('nextButton').classList.remove('hidden')}function next(){current<quiz.length-1?(current++,render()):results()}function results(){clearInterval(timer);const pct=Math.round(score/quiz.length*100);app.innerHTML=`<div class="shell"><div class="stage">${top()}<div class="content" style="text-align:center"><h2 class="headline">${t('finalTitle')}</h2><p>${t('finalText')}</p><div class="finalScore">${score} / ${quiz.length}</div><p>${pct}%</p><button id="restart" class="primary blueBtn">${t('restart')}</button></div><div class="footer"><span>CoR gamification prototype V1.2</span><span>Ready for GitHub Pages</span></div></div></div>`;bindLang();document.getElementById('restart').onclick=start}document.addEventListener('DOMContentLoaded',home);